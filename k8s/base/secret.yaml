# Secret compartilhado — referenciado pelos Deployments via envFrom/secretKeyRef.
# ATENÇÃO: valores DEMO (iguais ao docker-compose, só para o ambiente acadêmico). Em produção, NÃO
# versionar segredo real — usar Sealed Secrets / External Secrets / Key Vault, etc.
apiVersion: v1
kind: Secret
metadata:
  name: mba-secrets
  namespace: mba
type: Opaque
stringData:
  # Chave JWT — dois nomes porque os serviços divergem (Auth/Aluno/BFF/MVC vs Conteúdo/Pagamentos).
  AppSettings__Secret: "9F3KxR7QmL2WJ8ZpE0YB5aTgN1C4H6Md"
  AppSettings__JwtSettings__Secret: "9F3KxR7QmL2WJ8ZpE0YB5aTgN1C4H6Md"

  # Senha do SA do SQL Server (usada pelo Deployment do mssql e pelas connection strings, quando SqlServer).
  SA_PASSWORD: "MbaModulo4_Str0ng!Pass"
